{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node ./scripts/preinstall.mjs",
    "deploy:rx:up": "docker compose -f deploy/rx/docker-compose.yml up -d --build",
    "deploy:rx:down": "docker compose -f deploy/rx/docker-compose.yml down",
    "deploy:rx:init": "docker compose -f deploy/rx/docker-compose.yml --profile init run --rm db-init",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "tsx": "catalog:",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "stripe": "^22.1.0"
  }
}
