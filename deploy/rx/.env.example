# Copy to deploy/rx/.env and adjust as needed
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=pharmacare
POSTGRES_PORT=5432
API_PORT=5000
RX_PORT=5174

# NHS TRUD — dm+d weekly sync (server-side only; never expose to PMR client)
TRUD_API_KEY=
TRUD_DMD_ITEM_ID=24
